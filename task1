#include<stdio.h>
int main() {

   float p = 3.14 ;
   float s , r , v ;

    printf("radius=");
    scanf("%f", &r);
    v = 4 / 3 * p * r * r * r;
    s = 4 * r * r * p;
    printf("s=%.2f\n" , s);
    printf("v=%.2f\n" , v);

    return 0;
}
